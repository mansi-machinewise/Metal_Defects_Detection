# =============================================================================
# requirements.txt — Phase 1 Dependencies
# Pinned for reproducibility. Update versions carefully.
# =============================================================================

# Core ML
ultralytics==8.3.40         # YOLOv8 (includes torch dependency management)
torch>=2.0.0                # PyTorch — GPU or CPU wheel installed separately
torchvision>=0.15.0

# Computer Vision
opencv-python-headless==4.10.0.84   # OpenCV without GUI (use full if displaying windows)
Pillow>=10.0.0

# Data & Configuration
numpy==1.26.4
pandas>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0
psutil>=5.9.0               # RAM monitoring
GPUtil>=1.4.0               # GPU monitoring (optional, graceful fallback)
rich>=13.0.0                # Pretty console output

# Dev / Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Phase 2+ — FastAPI Backend
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
Pillow>=10.0.0

# FastAPI Backend
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9